# Aider Notes

Include `skills/journeys/SKILL.md` in Aider's read-only context (`/add` the
file) or `.aider.conf.yml` `read:` list when working on Journeys in this repo.

Recommended activation prompt:

```text
/skills/journeys/SKILL.md — follow Journeys schema 1.0, structured conditions,
and @journeys/core APIs. Validate workflow JSON after edits.
```

Canonical instructions: [../SKILL.md](../SKILL.md).
